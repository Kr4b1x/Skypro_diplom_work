from django.apps import AppConfig


class TrainmoduleConfig(AppConfig):
    """
    Config for the training module app.
    """
    default_auto_field = "django.db.models.BigAutoField"
    name = "trainmodule"
